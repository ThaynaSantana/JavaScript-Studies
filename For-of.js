// For...of

let name = 'Teo'
let names = ['Marquinhos', 'Nelo', 'Del']

// Imprimirá cara caractere da variavel name 
for (let char of name) {
	console.log(char)
}

// Imprimira os elementos do array 
for (let name of names) {
	console.log(name)
}